<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.NoryptProtect" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/norypt_bg</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
    </style>

    <!-- Lockdown mode's blank home: pure black, nothing drawn until the owner holds and enters the PIN. -->
    <style name="Theme.NoryptProtect.Blank" parent="Theme.NoryptProtect">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
