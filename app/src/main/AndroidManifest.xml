<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!-- DELIBERATELY NOT DECLARED: android.permission.INTERNET -->
    <permission android:name="com.norypt.protect.permission.TRIGGER"
                android:protectionLevel="signature"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission android:name="android.permission.RECEIVE_SMS"/>
    <!-- RECEIVE_SMS otherwise implies telephony is required, which would exclude
         tablets and other non-telephony devices. Only the A6 secret-SMS trigger
         needs it; every other protection works without a radio. -->
    <uses-feature android:name="android.hardware.telephony" android:required="false"/>
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
                     tools:ignore="ProtectedPermissions"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>

    <!-- Required by B5 (package-internet watcher) on Android 11+. Without this,
         getInstalledPackages() hides 3rd-party apps and PACKAGE_ADDED broadcasts
         for those apps are never delivered to our receiver. No Play Store target,
         so we take the honest route instead of splitting into <queries> filters. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
                     tools:ignore="QueryAllPackagesPermission"/>

    <!-- ADB-granted only (signature|privileged — cannot be requested at runtime).
         Needed so the app can read AND write protected Settings.Secure keys,
         specifically emergency_gesture_enabled used by C2 auto-disable SOS.
         Grant with:
           adb shell pm grant com.norypt.protect android.permission.WRITE_SECURE_SETTINGS -->
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS"
                     tools:ignore="ProtectedPermissions"/>

    <application
        android:name=".NoryptProtectApp"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.NoryptProtect"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules">

        <activity
            android:name=".service.PowerMenuBlockerActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"/>

        <activity
            android:name=".service.WipeCountdownActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.NoryptProtect"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.NoryptProtect"/>

        <activity-alias
            android:name=".LauncherAlias"
            android:targetActivity=".MainActivity"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <meta-data android:name="android.app.shortcuts" android:resource="@xml/shortcuts"/>
        </activity-alias>

        <receiver
            android:name=".admin.ProtectAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies"/>
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED"/>
            </intent-filter>
        </receiver>

        <receiver
            android:name=".service.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <service
            android:name=".service.ProtectForegroundService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Runs the Norypt Protect arming monitors (unlocked-timer, fake-messenger, etc.)."/>
        </service>

        <!-- UserPresentReceiver is registered at runtime by UserPresentMonitor:
             ACTION_USER_PRESENT is not an implicit-broadcast exception, so a
             manifest-declared receiver is never invoked on API 26+. -->

        <!-- UsbLockedMonitor receiver is registered at runtime against
             android.hardware.usb.action.USB_STATE so it sees the data flags;
             ACTION_POWER_CONNECTED used to live here but it fires on dumb
             chargers too, which is wrong for this trigger. -->

        <!-- android:permission restricts the sender: SMS_RECEIVED is a protected
             broadcast, but without this any UID the platform trusts with protected
             broadcasts could deliver a fabricated PDU and wipe the device. -->
        <receiver
            android:name=".triggers.SmsSecretReceiver"
            android:permission="android.permission.BROADCAST_SMS"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.provider.Telephony.SMS_RECEIVED"/>
            </intent-filter>
        </receiver>

        <receiver
            android:name=".triggers.ExternalTriggerReceiver"
            android:permission="com.norypt.protect.permission.TRIGGER"
            android:exported="true">
            <intent-filter>
                <action android:name="com.norypt.protect.action.TRIGGER"/>
            </intent-filter>
        </receiver>

        <receiver
            android:name=".triggers.ExternalPanicReceiver"
            android:permission="com.norypt.protect.permission.TRIGGER"
            android:exported="true">
            <intent-filter>
                <action android:name="info.guardianproject.panic.action.TRIGGER"/>
            </intent-filter>
        </receiver>

        <service android:name=".service.NoryptNotificationListenerService"
                 android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
                 android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService"/>
            </intent-filter>
        </service>

        <service android:name=".service.PanicTileService"
                 android:label="Norypt Panic"
                 android:icon="@mipmap/ic_launcher"
                 android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
                 android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
        </service>

    </application>
</manifest>
